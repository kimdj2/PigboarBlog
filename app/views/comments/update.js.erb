$comment_form = $("#comment_<%= @comment.id %> .comment-form form");
$comment = $("#comment_<%= @comment.id %>");
<% if @comment.errors.empty? %>
  $comment.after("<%=j render @comment %>").find(".media").last().effect("highlight", {}, 1000);
  $comment.remove();
  //$comment_form[0].remove();
<% else  %>
  alert("このコメントは既に削除されています。");
<% end %>
$('.ajax_loading').hide();